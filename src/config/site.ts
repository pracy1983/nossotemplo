// Configurações globais do site
export const SITE_CONFIG = {
  // URL base do site em produção
  BASE_URL: 'https://nossotemplo.com.br',
  
  // Caminhos das rotas
  ROUTES: {
    INVITE: '/convite',
    RESET_PASSWORD: '/redefinir-senha'
  }
} as const;
